{
  "name": "solaride",
  "version": "1.0.0",
  "private": true,
  "description": "SOLARIDE — rooftop solar panel installation across North India. Static marketing website.",
  "type": "module",
  "scripts": {
    "generate:media": "node scripts/generate-media.mjs",
    "render:media": "node scripts/render-media.mjs",
    "build": "node scripts/build.mjs",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "playwright": "1.63.0",
    "sharp": "0.34.5",
    "tailwindcss": "3.4.17",
    "three": "0.180.0",
    "vitest": "^2.1.9"
  }
}
